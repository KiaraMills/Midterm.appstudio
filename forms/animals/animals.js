let myAnimals = ['dog', 'cat', 'horse', 'meerkat']
  console.log(myAnimals)
  
let message = prompt("What is the name of the animal?")
console.log(message)
let userAnswer = `${message}`
myAnimals.push()()

alert(`The last animal is a/an ${userAnswer}.`)

while (i <= message) {
  console.log(`This is loop iteration ${i}`)
  (i += 2)