let bucketList= ()
console.log(bucketList)

let message = prompt('Do you want to see your bucketlist or add new item?')
console.log("`${bucketList}` added!")


for (i = 0; i < bucketList.length; i++)
  console.log(`${bucketList[i]}`)